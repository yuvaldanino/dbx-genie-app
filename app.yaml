command:
  - uvicorn
  - genieapp.backend.app:app
  - --host
  - 0.0.0.0
  - --port
  - "8000"

env:
  - name: PYTHONPATH
    value: src
  - name: STATE_FILE_PATH
    value: /Volumes/yd_launchpad_final_classic_catalog/genie_app/raw_data/state.json
  - name: DATABRICKS_WAREHOUSE_ID
    valueFrom: sql-warehouse
